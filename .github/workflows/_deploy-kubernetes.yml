# @format
# @description Reusable Kubernetes Infrastructure Deployment Workflow
#
# ─── Purpose ───────────────────────────────────────────────────────
# Deploys the 9-stack K8s (kubeadm) infrastructure via CDK.
# Provisions AWS resources; does NOT bootstrap Kubernetes.
#
# ─── Stack Deployment Order (10 stacks) ────────────────────────────
#   0.  ✅ Verify BFF ECR  (pre-flight: confirm admin-api ECR repo exists
#                           in Shared-Infra before any container push job)
#   1.  Data             (DynamoDB + S3 + SSM Parameters)
#   1b. ✅ Verify Data    (Integration test — DynamoDB, S3, SSM, CFn outputs)
#   2.  Base             (VPC networking + SG + KMS + EBS + EIP)
#   2b. ✅ Verify Base    (Integration test — VPC, SGs, KMS, EBS, EIP, Route53)
#   2c. API              (API Gateway + Lambda) [parallel with 2, depends on 1]
#   2d. Observability    (CloudWatch dashboard) [parallel with 2c, depends on 2b]
#   2e. PlatformRds      (PostgreSQL 18 + SM secret + SSM params) [parallel, depends on 2b]
#   3.  Compute          (Control Plane EC2 + ASG + AMI-refresh EventBridge/SFN)
#   3b. General Pool ASG (Kubernetes-native worker nodes — general purpose)
#   3c. Monitoring Pool  (Kubernetes-native worker nodes — observability taint)
#   4.  AppIam           (Application-tier IAM grants)
#   5.  Edge             (CloudFront + ACM + WAF in us-east-1)
#   5b. ✅ Verify Edge    (Integration test — behaviour order, auth cookies, Set-Cookie)
#
# ─── Post-Deploy ────────────────────────────────────────────────────
#   6.  Deployment Failure Alert    (halt + preserve + alert)
#   7.  Summary
#
# ─── Concurrency ───────────────────────────────────────────────────
#   This is a workflow_call (reusable) workflow — concurrency MUST be set
#   on the caller (e.g., deploy-kubernetes.yml).
#   Each caller defines: concurrency: { group: deploy-k8s-<env>, cancel-in-progress: false }

name: Deploy K8s (Reusable)

on:
  workflow_call:
    inputs:
      environment:
        description: "Target environment (development, staging, production)"
        required: true
        type: string
      cdk-environment:
        description: "CDK environment name"
        required: true
        type: string
      require-approval:
        description: "CDK deployment approval mode"
        required: false
        default: "never"
        type: string
      skip-verification:
        description: "Skip post-deployment verification"
        required: false
        default: false
        type: boolean
      domain-name:
        description: "Domain name for CloudFront Edge stack (optional — falls back to vars.DOMAIN_NAME)"
        required: false
        type: string
        default: ""
      hosted-zone-id:
        description: "Route53 Hosted Zone ID (optional — falls back to vars.HOSTED_ZONE_ID)"
        required: false
        type: string
        default: ""
      cross-account-role-arn:
        description: "Cross-account IAM role for Route53 (optional — falls back to vars.DNS_VALIDATION_ROLE)"
        required: false
        type: string
        default: ""


    secrets:
      AWS_OIDC_ROLE:
        description: "AWS OIDC role ARN"
        required: false
      VERIFICATION_SECRET:
        required: false
      ALLOW_IPV4:
        description: "IPv4 CIDR to allowlist in WAF (e.g., 203.0.113.42/32)"
        required: false
      ALLOW_IPV6:
        description: "IPv6 CIDR to allowlist in WAF (e.g., 2a02:8084::/128)"
        required: false
      TUCAKEN_IO_HOSTED_ZONE_ID:
        description: "Route53 hosted zone ID for tucaken.io (only required when vars.TUCAKEN_EDGE_ENABLED=true)"
        required: false
      TUCAKEN_COM_HOSTED_ZONE_ID:
        description: "Route53 hosted zone ID for tucaken.com (only required when vars.TUCAKEN_EDGE_ENABLED=true)"
        required: false

env:
  NODE_VERSION: "22"
  AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
  DEPLOY_ENVIRONMENT: ${{ inputs.environment }}
  # Force Node 24 for actions that haven't released Node 24-native versions yet
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ===========================================================================
  # Setup, Build & Synthesize
  # ===========================================================================
  setup:
    name: Setup & Synthesize
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 20
    environment: ${{ inputs.environment }}
    outputs:
      commit-sha: ${{ github.sha }}
      commit-short-sha: ${{ steps.pipeline-setup.outputs.short-sha }}
      node-version: ${{ steps.setup-tools.outputs.node-version }}
      # Stack names from synthesize-ci.ts (10-stack architecture)
      data-stack: ${{ steps.synth.outputs.data }}
      base-stack: ${{ steps.synth.outputs.base }}
      controlplane-stack: ${{ steps.synth.outputs.controlPlane }}
      # ASG pool stacks — Kubernetes-native worker nodes
      general-pool-stack: ${{ steps.synth.outputs.generalPool }}
      monitoring-pool-stack: ${{ steps.synth.outputs.monitoringPool }}
      appiam-stack: ${{ steps.synth.outputs.appIam }}
      api-stack: ${{ steps.synth.outputs.api }}
      edge-stack: ${{ steps.synth.outputs.edge }}
      tucaken-edge-stack: ${{ steps.synth.outputs.tucakenEdge }}
      tucaken-enabled: ${{ steps.detect-tucaken.outputs.enabled }}
      observability-stack: ${{ steps.synth.outputs.observability }}
      platform-rds-stack: ${{ steps.synth.outputs.platformRds }}


    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Environment and Dependencies
        id: setup-tools
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Build TypeScript
        run: just build

      - name: Pipeline Setup (validate, resolve, mask)
        id: pipeline-setup
        env:
          AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          INPUT_DOMAIN: ${{ inputs.domain-name }}
          INPUT_HZ_ID: ${{ inputs.hosted-zone-id }}
          INPUT_ROLE_ARN: ${{ inputs.cross-account-role-arn }}
          VARS_DOMAIN: ${{ vars.DOMAIN_NAME }}
          VARS_HZ_ID: ${{ vars.HOSTED_ZONE_ID }}
          VARS_ROLE_ARN: ${{ vars.DNS_VALIDATION_ROLE }}
        run: just ci-pipeline-setup

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Synthesize & Determine Stack Names
        id: synth
        env:
          DOMAIN_NAME: ${{ steps.pipeline-setup.outputs.domain-name }}
          HOSTED_ZONE_ID: ${{ steps.pipeline-setup.outputs.hosted-zone-id }}
          CROSS_ACCOUNT_ROLE_ARN: ${{ steps.pipeline-setup.outputs.cross-account-role-arn }}
          NOTIFICATION_EMAIL: ${{ vars.NOTIFICATION_EMAIL }}
          SES_FROM_EMAIL: ${{ vars.SES_FROM_EMAIL }}
          VERIFICATION_SECRET: ${{ secrets.VERIFICATION_SECRET }}
          VERIFICATION_BASE_URL: ${{ vars.VERIFICATION_BASE_URL }}
          # ── WAF IP Allowlist (Pre-launch Access Restriction) ─────────
          # Restricts CloudFront access to listed IPs only.
          #
          # GitHub Environment settings (Settings → Environments → development):
          #   Variable: RESTRICT_ACCESS = "true" | "false"
          #   Secret:   ALLOW_IPV4 = "<your-ipv4>/32"  (e.g., "203.0.113.42/32")
          #   Secret:   ALLOW_IPV6 = "<your-ipv6>/128"  (e.g., "2a02:8084::/128")
          #
          # To go live: set RESTRICT_ACCESS to "false" in GitHub Environment
          # variables and re-run the pipeline. No code change, commit, or PR
          # needed — the WAF default action flips from BLOCK to ALLOW.
          #
          # To lock down again: set RESTRICT_ACCESS back to "true".
          # ──────────────────────────────────────────────────────────────
          RESTRICT_ACCESS: ${{ vars.RESTRICT_ACCESS }}
          ALLOW_IPV4: ${{ secrets.ALLOW_IPV4 }}
          ALLOW_IPV6: ${{ secrets.ALLOW_IPV6 }}
          # ── Tucaken Edge (independent SaaS edge — tucaken.io / tucaken.com) ─
          # When all four are set, the kubernetes factory instantiates
          # TucakenEdgeStack alongside the portfolio Edge stack. Missing any
          # value is a soft-skip (factory logs an info annotation).
          TUCAKEN_IO_DOMAIN: ${{ vars.TUCAKEN_IO_DOMAIN }}
          TUCAKEN_COM_DOMAIN: ${{ vars.TUCAKEN_COM_DOMAIN }}
          TUCAKEN_IO_HOSTED_ZONE_ID: ${{ secrets.TUCAKEN_IO_HOSTED_ZONE_ID }}
          TUCAKEN_COM_HOSTED_ZONE_ID: ${{ secrets.TUCAKEN_COM_HOSTED_ZONE_ID }}
        run: just ci-synth kubernetes ${{ inputs.cdk-environment }}

      # Detect whether the Tucaken edge stack was actually emitted by synth.
      # The synth output `tucakenEdge` always contains the stack name from the
      # registry (unconditional), so we can't gate on it. Instead, check for
      # the synthesized template file — present only when the factory created
      # the stack (i.e. TUCAKEN_*_HOSTED_ZONE_ID secrets were resolved).
      #
      # The diagnostic block prints whether each secret/var was non-empty when
      # the synth step ran — without leaking values. If the template is missing
      # but secrets show as "set", check the factory soft-skip criteria
      # (CROSS_ACCOUNT_ROLE_ARN must also be set).
      - name: Detect Tucaken Edge enablement
        id: detect-tucaken
        shell: bash
        env:
          TUCAKEN_STACK_NAME: ${{ steps.synth.outputs.tucakenEdge }}
          DIAG_IO_ZONE: ${{ secrets.TUCAKEN_IO_HOSTED_ZONE_ID != '' && 'set' || 'unset' }}
          DIAG_COM_ZONE: ${{ secrets.TUCAKEN_COM_HOSTED_ZONE_ID != '' && 'set' || 'unset' }}
          DIAG_DNS_ROLE: ${{ vars.DNS_VALIDATION_ROLE != '' && 'set' || 'unset' }}
          DIAG_IO_DOMAIN: ${{ vars.TUCAKEN_IO_DOMAIN != '' && 'set (overridden)' || 'unset (uses default tucaken.io)' }}
          DIAG_COM_DOMAIN: ${{ vars.TUCAKEN_COM_DOMAIN != '' && 'set (overridden)' || 'unset (uses default tucaken.com)' }}
        run: |
          set -euo pipefail
          echo "── Tucaken Edge inputs (presence-only diagnostic) ──"
          echo "  TUCAKEN_IO_HOSTED_ZONE_ID  : ${DIAG_IO_ZONE}"
          echo "  TUCAKEN_COM_HOSTED_ZONE_ID : ${DIAG_COM_ZONE}"
          echo "  DNS_VALIDATION_ROLE (var)  : ${DIAG_DNS_ROLE}"
          echo "  TUCAKEN_IO_DOMAIN          : ${DIAG_IO_DOMAIN}"
          echo "  TUCAKEN_COM_DOMAIN         : ${DIAG_COM_DOMAIN}"
          echo ""

          TEMPLATE="infra/cdk.out/${TUCAKEN_STACK_NAME}.template.json"
          if [[ -f "$TEMPLATE" ]]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "[OK] Found ${TEMPLATE} → enabled=true"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "[INFO] ${TEMPLATE} not found → enabled=false"
            echo "       (factory soft-skipped because at least one of:"
            echo "         TUCAKEN_IO_HOSTED_ZONE_ID, TUCAKEN_COM_HOSTED_ZONE_ID,"
            echo "         CROSS_ACCOUNT_ROLE_ARN was empty at synth time)"
          fi

      - name: Cache Synthesized Templates
        uses: actions/cache/save@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: infra/cdk.out/
          key: cdk-synth-k8s-${{ inputs.cdk-environment }}-${{ github.run_id }}-${{ github.run_attempt }}




  # ===========================================================================
  # Template Validation (cfn-lint)
  # ===========================================================================
  validate-templates:
    name: Validate Templates
    needs: [setup]
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10

    steps:
      - name: Checkout Config
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          sparse-checkout: .cfnlintrc
          sparse-checkout-cone-mode: false

      - name: Restore Synthesized Templates
        uses: actions/cache/restore@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: infra/cdk.out/
          key: cdk-synth-k8s-${{ inputs.cdk-environment }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Validate CloudFormation Templates
        shell: python3 {0}
        run: |
          import sys, glob
          sys.setrecursionlimit(10000)
          from cfnlint import decode, runner
          from cfnlint.config import ConfigMixIn

          templates = sorted(glob.glob("infra/cdk.out/**/*.template.json", recursive=True))
          print(f"Validating {len(templates)} templates...")
          total_errors = 0
          for tpl in templates:
              print(f"\n--- {tpl} ---")
              try:
                  config = ConfigMixIn(["--template", tpl])
                  matches = list(runner.Runner(config).run())
                  if matches:
                      total_errors += len(matches)
                      for match in matches:
                          print(f"  {match}")
                  else:
                      print("  PASS")
              except RecursionError:
                  print(f"  SKIP (template too deeply nested)")
              except Exception as e:
                  print(f"  SKIP ({type(e).__name__}: {e})")

          if total_errors > 0:
              print(f"\n{total_errors} validation errors found")
              sys.exit(1)
          else:
              print(f"\nAll {len(templates)} templates passed validation")

  # ===========================================================================
  # Verify BFF ECR Repositories — Pre-flight Gate (0)
  #
  # Confirms that both BFF ECR repositories (public-api and admin-api) exist
  # in the Shared-Infra stack BEFORE any downstream deploy jobs run. Reads the
  # SSM parameters published by SharedVpcStack:
  #   /shared/ecr-public-api/{env}/repository-uri
  #   /shared/ecr-admin-api/{env}/repository-uri
  #
  # If either parameter is missing the Shared-Infra stack has not been deployed
  # yet. Run deploy-shared.yml first, then re-trigger this pipeline.
  # ===========================================================================
  verify-bff-ecr:
    name: "Verify BFF ECR"
    needs: [setup, validate-templates]
    if: always() && needs.setup.result == 'success' && needs.validate-templates.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 5
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Confirm BFF ECR repositories exist in SSM
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        # Reads both BFF ECR SSM parameters to provide information about the repositories.
        # These are written by SharedVpcStack when Shared-Infra is deployed.
        # This will not block the pipeline during a day 0 launch.
        shell: bash
        run: |
          set -uo pipefail

          PUBLIC_API_SSM="/shared/ecr-public-api/${CDK_ENV}/repository-uri"
          ADMIN_API_SSM="/shared/ecr-admin-api/${CDK_ENV}/repository-uri"

          echo "Checking SSM parameter: ${PUBLIC_API_SSM}"
          PUBLIC_API_URI=$(aws ssm get-parameter \
            --name "${PUBLIC_API_SSM}" \
            --region "${AWS_REGION}" \
            --query 'Parameter.Value' \
            --output text 2>&1) || {
            echo "::warning::public-api ECR repository not found in SSM."
            echo "::warning::SSM path: ${PUBLIC_API_SSM}"
            echo "::warning::Day 0 deployment - assuming cluster launch."
            PUBLIC_API_URI="not-found"
          }

          echo "Checking SSM parameter: ${ADMIN_API_SSM}"
          ADMIN_API_URI=$(aws ssm get-parameter \
            --name "${ADMIN_API_SSM}" \
            --region "${AWS_REGION}" \
            --query 'Parameter.Value' \
            --output text 2>&1) || {
            echo "::warning::admin-api ECR repository not found in SSM."
            echo "::warning::SSM path: ${ADMIN_API_SSM}"
            echo "::warning::Day 0 deployment - assuming cluster launch."
            ADMIN_API_URI="not-found"
          }

          echo "public-api ECR URI : ${PUBLIC_API_URI}"
          echo "admin-api  ECR URI : ${ADMIN_API_URI}"
          echo "BFF ECR pre-flight check completed."

  # ===========================================================================
  # Deploy Data Stack (1/6) — DynamoDB + S3 + SSM Parameters
  # ===========================================================================
  deploy-data:
    name: Deploy Data
    needs: [setup, validate-templates, verify-bff-ecr]
    if: >-
      always()
      && needs.setup.result == 'success'
      && needs.validate-templates.result == 'success'
      && needs.verify-bff-ecr.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.data-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Verify Data Stack — Integration Tests Gate
  #
  # Runs integration tests that call real AWS APIs to validate the data
  # stack deployment. Reads SSM parameters published by the stack and
  # confirms DynamoDB, S3, and CloudFormation outputs are correctly
  # configured. Gates the base stack deployment.
  # ===========================================================================
  verify-data-stack:
    name: Verify Data Stack
    needs: [setup, deploy-data]
    if: always() && needs.deploy-data.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Data Stack Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: just ci-integration-test kubernetes/data-stack $CDK_ENV --verbose

  # ===========================================================================
  # Deploy API Stack — API Gateway + Lambda (depends on Data only)
  #
  # Deploys in parallel with Base. The API stack only depends on the Data
  # stack (DynamoDB table + SSM parameters). It is an application-layer
  # stack that can be deployed independently from core infrastructure.
  # ===========================================================================
  deploy-api:
    name: Deploy API
    needs: [setup, deploy-data, verify-data-stack]
    if: always() && needs.deploy-data.result == 'success' && needs.verify-data-stack.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.api-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
      # Email config — passed to CDK synth inside _deploy-stack.yml
      notification-email: ${{ vars.NOTIFICATION_EMAIL }}
      ses-from-email: ${{ vars.SES_FROM_EMAIL }}
    secrets: inherit

  # ===========================================================================
  # Deploy Base Stack (2) — VPC networking + SG + KMS + EBS + EIP
  # ===========================================================================
  deploy-base:
    name: Deploy Base
    needs: [setup, deploy-data, verify-data-stack]
    if: always() && needs.deploy-data.result == 'success' && needs.verify-data-stack.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.base-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Verify Base Stack — Integration Tests Gate
  #
  # Runs integration tests that call real AWS APIs to validate the base
  # stack deployment. Reads SSM parameters published by the stack and
  # confirms VPC, SGs, KMS, EBS, EIP, S3, and Route 53 resources exist
  # and are correctly configured. Gates all downstream stacks.
  # ===========================================================================
  verify-base-stack:
    name: Verify Base Stack
    needs: [setup, deploy-base]
    if: always() && needs.deploy-base.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Base Stack Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: just ci-integration-test kubernetes/base-stack $CDK_ENV --verbose

  # ===========================================================================
  # Deploy Compute Stack — Control Plane EC2 + ASG
  # ===========================================================================
  deploy-controlplane:
    name: Deploy Control Plane
    needs: [setup, deploy-base, verify-base-stack]
    if: >-
      always()
      && needs.deploy-base.result == 'success'
      && needs.verify-base-stack.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.controlplane-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Verify Control Plane Stack — Integration Tests Gate
  #
  # Runs integration tests that call real AWS APIs to validate the control
  # plane deployment. Verifies EIP association to the running instance,
  # security group attachment (cluster base, control plane, ingress), port
  # rules on each SG, and CloudFormation outputs. Gates worker deployments.
  # ===========================================================================
  verify-controlplane-stack:
    name: Verify Control Plane
    needs: [setup, deploy-controlplane]
    if: always() && needs.deploy-controlplane.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Control Plane Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: just ci-integration-test kubernetes/control-plane-stack $CDK_ENV --verbose

  # ===========================================================================
  # Smoke Test AMI Refresh Lambdas (3b-gate)
  #
  # Invokes each of the three AMI refresh Lambda functions (UpdateLaunchTemplate,
  # StartInstanceRefresh, CheckRefreshStatus) with a canary payload immediately
  # after the ControlPlane stack is deployed. The test does NOT require real SSM
  # data — it only distinguishes "crashes before SDK init" (TypeError = bundling
  # regression) from "reaches real business logic" (SSM miss = Lambda is alive).
  #
  # Gates both worker pool deployments: if the Lambdas are broken, rolling
  # worker instances after an AMI bake will silently fail.
  # ===========================================================================
  smoke-test-ami-refresh:
    name: Smoke Test AMI Refresh Lambdas
    needs: [setup, deploy-controlplane]
    if: always() && needs.deploy-controlplane.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 5
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Discover AMI Refresh Lambda Functions
        id: discover
        shell: bash
        env:
          STACK_NAME: ${{ needs.setup.outputs.controlplane-stack }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: |
          set -euo pipefail

          LAMBDAS=$(aws cloudformation list-stack-resources \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --query 'StackResourceSummaries[?ResourceType==`AWS::Lambda::Function` && contains(LogicalResourceId, `AmiRefresh`)].PhysicalResourceId' \
            --output json | jq -c '.')

          COUNT=$(echo "$LAMBDAS" | jq 'length')
          if [ "$COUNT" -lt 3 ]; then
            echo "::error::Expected 3 AmiRefresh Lambdas in $STACK_NAME, found $COUNT"
            aws cloudformation list-stack-resources \
              --stack-name "$STACK_NAME" \
              --region "$AWS_REGION" \
              --query 'StackResourceSummaries[?ResourceType==`AWS::Lambda::Function`].[LogicalResourceId,PhysicalResourceId]' \
              --output table
            exit 1
          fi

          echo "Discovered $COUNT AMI refresh Lambda functions:"
          echo "$LAMBDAS" | jq -r '.[]' | sed 's/^/  /'
          echo "lambda_names=$LAMBDAS" >> "$GITHUB_OUTPUT"

      - name: Invoke AMI Refresh Lambdas (canary)
        shell: bash
        env:
          LAMBDA_NAMES: ${{ steps.discover.outputs.lambda_names }}
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: |
          set -euo pipefail

          PAYLOAD='{"paramName":"/k8s/'"$CDK_ENV"'/golden-ami/latest","role":"workers"}'
          FAILED=0

          while IFS= read -r FN; do
            echo ""
            echo "── Invoking: $FN"

            aws lambda invoke \
              --function-name "$FN" \
              --payload "$PAYLOAD" \
              --cli-binary-format raw-in-base64-out \
              --region "$AWS_REGION" \
              /tmp/response.json \
              --output json > /tmp/invoke_result.json

            STATUS=$(jq -r '.StatusCode' /tmp/invoke_result.json)
            FUNCTION_ERROR=$(jq -r '.FunctionError // empty' /tmp/invoke_result.json)

            if [ "$STATUS" != "200" ]; then
              echo "::error::$FN returned HTTP $STATUS — Lambda service error"
              FAILED=1
              continue
            fi

            if [ -n "$FUNCTION_ERROR" ]; then
              ERROR_TYPE=$(jq -r '.errorType // ""' /tmp/response.json)
              ERROR_MSG=$(jq -r '.errorMessage // ""' /tmp/response.json | head -c 200)

              if echo "$ERROR_TYPE" | grep -qE "^(TypeError|ReferenceError|SyntaxError)$"; then
                echo "::error::$FN crashed with $ERROR_TYPE: $ERROR_MSG"
                echo "::error::Handler fails before any business logic — likely a bundling or client-instantiation regression."
                FAILED=1
              else
                echo "  ⚠ Business error (Lambda is alive): $ERROR_TYPE — $ERROR_MSG"
                echo "  Expected when SSM parameters are not yet populated."
              fi
            else
              echo "  ✓ $FN succeeded"
            fi
          done < <(echo "$LAMBDA_NAMES" | jq -r '.[]')

          if [ "$FAILED" != "0" ]; then
            echo ""
            echo "One or more AMI refresh Lambdas failed the canary check."
            echo "Check CloudWatch logs: aws logs tail /aws/lambda/<fn-name> --since 10m --region $AWS_REGION"
            exit 1
          fi

          echo ""
          echo "All AMI refresh Lambdas passed the canary check."

  # ===========================================================================
  # Deploy General Pool ASG Stack (3b) — General-Purpose ASG Pool
  #
  # Kubernetes-native KubernetesWorkerAsgStack (no taint).
  # Hosts Next.js, start-admin, ArgoCD, and system components.
  # ===========================================================================
  deploy-general-pool:
    name: Deploy General Pool ASG
    needs: [setup, deploy-controlplane, verify-controlplane-stack, smoke-test-ami-refresh]
    if: >-
      always()
      && needs.deploy-controlplane.result == 'success'
      && needs.verify-controlplane-stack.result == 'success'
      && needs.smoke-test-ami-refresh.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.general-pool-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Verify General Pool ASG Stack — Validate Launch Template, ASG, IAM, SSM tags
  # ===========================================================================
  verify-general-pool-stack:
    name: Verify General Pool ASG
    needs: [setup, deploy-general-pool]
    if: always() && needs.deploy-general-pool.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run General Pool Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
          WORKER_POOL: general
        run: just ci-integration-test kubernetes/worker-asg-stack $CDK_ENV --testNamePattern="general-pool" --verbose

  # ===========================================================================
  # Deploy Monitoring Pool ASG Stack (3f) — Observability ASG Pool
  #
  # Parameterised KubernetesWorkerAsgStack for the monitoring pool.
  # Tainted `dedicated=monitoring:NoSchedule` by the bootstrap script.
  # Hosts Prometheus, Grafana, Loki, Tempo, and Alloy.
  # ===========================================================================
  deploy-monitoring-pool:
    name: Deploy Monitoring Pool ASG
    needs: [setup, deploy-controlplane, verify-controlplane-stack, smoke-test-ami-refresh]
    if: >-
      always()
      && needs.deploy-controlplane.result == 'success'
      && needs.verify-controlplane-stack.result == 'success'
      && needs.smoke-test-ami-refresh.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.monitoring-pool-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Verify Monitoring Pool ASG Stack — Validate Launch Template, ASG, IAM, SNS, SSM tags
  # ===========================================================================
  verify-monitoring-pool-stack:
    name: Verify Monitoring Pool ASG
    needs: [setup, deploy-monitoring-pool]
    if: always() && needs.deploy-monitoring-pool.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Monitoring Pool Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
          WORKER_POOL: monitoring
        run: just ci-integration-test kubernetes/worker-asg-stack $CDK_ENV --testNamePattern="monitoring-pool" --verbose

  # ===========================================================================
  # Verify Launch Template AMI Consistency
  #
  # Confirms that every compute Launch Template (control-plane, general-pool,
  # monitoring-pool) references the same AMI ID currently published to SSM
  # by the kubernetes-bootstrap Golden AMI pipeline.
  #
  # A mismatch means the compute stack was not redeployed after the last AMI
  # bake — any future ASG replacement would launch the stale image. This job
  # runs in parallel with the individual pool verify jobs and gates AppIam.
  #
  # Vacuous pass when the SSM parameter does not yet exist (Day-0).
  # ===========================================================================
  verify-launch-template-ami:
    name: Verify Launch Template AMI
    needs:
      - setup
      - deploy-controlplane
      - deploy-general-pool
      - deploy-monitoring-pool
    if: >-
      always()
      && needs.deploy-controlplane.result == 'success'
      && needs.deploy-general-pool.result == 'success'
      && needs.deploy-monitoring-pool.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Launch Template AMI Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: just ci-integration-test kubernetes/launch-template-ami $CDK_ENV --verbose

  # ===========================================================================
  # Deploy AppIam Stack (4/8) — Application-tier IAM grants
  #
  # Hard-gated on both ASG pool verify jobs and the LT AMI check. If any pool
  # stack fails or a stale AMI is detected, IAM grants must not proceed.
  # ===========================================================================
  deploy-appiam:
    name: Deploy App IAM
    needs:
      - setup
      - verify-bff-ecr
      - deploy-general-pool
      - deploy-monitoring-pool
      - verify-general-pool-stack
      - verify-monitoring-pool-stack
      - verify-launch-template-ami
    if: >-
      always()
      && needs.verify-bff-ecr.result == 'success'
      && needs.deploy-general-pool.result == 'success'
      && needs.deploy-monitoring-pool.result == 'success'
      && needs.verify-general-pool-stack.result == 'success'
      && needs.verify-monitoring-pool-stack.result == 'success'
      && needs.verify-launch-template-ami.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.appiam-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit


  # ===========================================================================
  # Deploy Edge Stack (5/6) — CloudFront + ACM + WAF (us-east-1)
  # Origin: Elastic IP → Traefik
  #
  # PREREQUISITE: /k8s/{env}/cloudfront-origin-secret must exist in
  # eu-west-1 SSM before this job runs. Seed it manually on new environments:
  #   aws ssm put-parameter --name /k8s/{env}/cloudfront-origin-secret \
  #     --value "$(openssl rand -hex 32)" --type SecureString
  # ===========================================================================
  deploy-edge:
    name: Deploy Edge
    needs: [setup, deploy-appiam, deploy-data]
    if: always() && needs.deploy-appiam.result == 'success' && needs.deploy-data.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.edge-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.ROUTE53_REGION || 'us-east-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
      restrict-access: ${{ vars.RESTRICT_ACCESS }}
    secrets: inherit

  # ===========================================================================
  # Verify Edge Stack (5b) — Auth Cookie Forwarding + Behaviour Ordering
  #
  # Post-deployment integration tests that validate the LIVE CloudFront
  # distribution against the three CSRF root causes:
  #   1. Auth/admin behaviours use CookieBehavior: all (Set-Cookie passthrough)
  #   2. OriginRequestPolicy cookie names contain no wildcards
  #   3. Auth-sensitive patterns appear before /api/* catch-all
  #
  # This gate prevents auth regressions from reaching production.
  # ===========================================================================
  verify-edge-stack:
    name: Verify Edge Stack
    needs: [setup, deploy-edge]
    if: always() && needs.deploy-edge.result == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: infra

      - name: Run Edge Stack Integration Tests
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        run: just ci-integration-test kubernetes/edge-stack $CDK_ENV --verbose

  # ===========================================================================
  # Seed Tucaken CloudFront Origin Secret (5c)
  #
  # Idempotently provisions /k8s/{env}/tucaken-cloudfront-origin-secret as a
  # SecureString in eu-west-1 BEFORE deploy-tucaken-edge runs. CloudFront
  # forwards this secret as the X-CloudFront-Origin-Secret header; Traefik
  # validates it on the tucaken-app IngressRoute. Independent from the
  # portfolio origin secret — separate rotation lifecycle.
  #
  # Behaviour:
  #   - If parameter exists: leave value unchanged, log + skip
  #   - If missing: generate `openssl rand -hex 32` and create as SecureString
  #
  # Gated on vars.TUCAKEN_EDGE_ENABLED == 'true' so environments that don't
  # run tucaken (e.g. legacy staging) are unaffected.
  # ===========================================================================
  seed-tucaken-secret:
    name: Seed Tucaken Origin Secret
    needs: [setup]
    if: |
      always()
      && needs.setup.result == 'success'
      && needs.setup.outputs.tucaken-enabled == 'true'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 5
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}

      - name: Ensure tucaken-cloudfront-origin-secret exists
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
        shell: bash
        run: |
          set -euo pipefail
          PARAM_NAME="/k8s/${CDK_ENV}/tucaken-cloudfront-origin-secret"
          echo "Checking SSM parameter: ${PARAM_NAME}"

          if aws ssm get-parameter \
               --name "${PARAM_NAME}" \
               --region "${AWS_REGION}" \
               --with-decryption \
               --query 'Parameter.Name' \
               --output text >/dev/null 2>&1; then
            echo "[OK] Parameter already exists — leaving value unchanged"
            exit 0
          fi

          echo "[INFO] Parameter missing — generating new value"
          NEW_SECRET="$(openssl rand -hex 32)"
          # Mask before any potential echo path could reveal it
          echo "::add-mask::$NEW_SECRET"

          aws ssm put-parameter \
            --name "${PARAM_NAME}" \
            --value "${NEW_SECRET}" \
            --type SecureString \
            --description "Tucaken CloudFront origin secret — validated by Traefik HeadersRegexp on tucaken-app IngressRoute" \
            --tier Standard \
            --region "${AWS_REGION}" \
            --no-overwrite >/dev/null

          echo "[OK] Seeded ${PARAM_NAME} (SecureString)"

  # ===========================================================================
  # Deploy Tucaken Edge Stack (5d) — CloudFront + ACM + WAF (us-east-1)
  #
  # Independent customer-facing SaaS edge for tucaken.io / tucaken.com.
  # Two distributions: primary (.io) + redirect (.com → .io).
  # Origin: same Elastic IP as portfolio Edge — Traefik routes by Host header.
  #
  # Depends on:
  #   - deploy-base (writes EIP SSM in eu-west-1)
  #   - seed-tucaken-secret (writes origin secret SSM in eu-west-1)
  #
  # Gated on vars.TUCAKEN_EDGE_ENABLED == 'true'.
  # ===========================================================================
  deploy-tucaken-edge:
    name: Deploy Tucaken Edge
    needs: [setup, deploy-base, seed-tucaken-secret]
    if: |
      always()
      && needs.deploy-base.result == 'success'
      && needs.seed-tucaken-secret.result == 'success'
      && needs.setup.outputs.tucaken-enabled == 'true'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.tucaken-edge-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}
      aws-region: ${{ vars.ROUTE53_REGION || 'us-east-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
      restrict-access: ${{ vars.RESTRICT_ACCESS }}
    secrets: inherit

  # ===========================================================================
  # Diagnose Tucaken Edge Failure
  #
  # The CDK CLI surfaces "NoStack: CloudFormationStack object does not hold a
  # stack" when stack creation fails BEFORE any resource is successfully
  # created (typically during a custom resource invocation like ACM DNS-01
  # validation). The real root cause lives in the CloudFormation event log.
  # This job prints the failed events directly into the workflow output.
  # ===========================================================================
  diagnose-tucaken-edge:
    name: Diagnose Tucaken Edge Failure
    needs: [setup, deploy-tucaken-edge]
    if: |
      always()
      && needs.deploy-tucaken-edge.result == 'failure'
      && needs.setup.outputs.tucaken-enabled == 'true'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 5
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials (us-east-1)
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: us-east-1

      - name: Print failed CloudFormation events
        env:
          STACK_NAME: ${{ needs.setup.outputs.tucaken-edge-stack }}
        shell: bash
        run: |
          set -uo pipefail
          echo "── Stack status ─────────────────────────────────────"
          aws cloudformation describe-stacks \
            --region us-east-1 \
            --stack-name "$STACK_NAME" \
            --query 'Stacks[0].[StackStatus,StackStatusReason]' \
            --output table 2>/dev/null \
            || echo "(stack does not exist or already deleted)"

          echo ""
          echo "── Failed events (most recent first) ────────────────"
          aws cloudformation describe-stack-events \
            --region us-east-1 \
            --stack-name "$STACK_NAME" \
            --query 'StackEvents[?ResourceStatus==`CREATE_FAILED` || ResourceStatus==`UPDATE_FAILED` || ResourceStatus==`ROLLBACK_IN_PROGRESS`].[Timestamp,LogicalResourceId,ResourceType,ResourceStatusReason]' \
            --output table 2>/dev/null \
            || echo "(no events available)"

          echo ""
          echo "── Validation Lambda recent log streams ─────────────"
          LAMBDA_LOG_GROUP="/aws/lambda/tucaken-acm-dns-validation-${{ inputs.cdk-environment }}"
          aws logs describe-log-streams \
            --region us-east-1 \
            --log-group-name "$LAMBDA_LOG_GROUP" \
            --order-by LastEventTime --descending \
            --max-items 3 \
            --query 'logStreams[*].[logStreamName,lastEventTimestamp]' \
            --output table 2>/dev/null \
            || echo "(no log streams; Lambda may not have been invoked)"

          echo ""
          echo "── Latest validation Lambda log events ──────────────"
          LATEST_STREAM=$(aws logs describe-log-streams \
            --region us-east-1 \
            --log-group-name "$LAMBDA_LOG_GROUP" \
            --order-by LastEventTime --descending \
            --max-items 1 \
            --query 'logStreams[0].logStreamName' \
            --output text 2>/dev/null || echo "")
          if [[ -n "$LATEST_STREAM" && "$LATEST_STREAM" != "None" ]]; then
            aws logs get-log-events \
              --region us-east-1 \
              --log-group-name "$LAMBDA_LOG_GROUP" \
              --log-stream-name "$LATEST_STREAM" \
              --query 'events[*].message' \
              --output text 2>/dev/null | tail -100
          else
            echo "(no Lambda log stream found — Lambda likely never invoked or just created)"
          fi

  # ===========================================================================
  # Verify Tucaken Edge Stack (5e)
  #
  # Lightweight post-deploy check: confirms both distributions exist, ACM
  # certs are ISSUED, and the redirect distribution returns 301 → tucaken.io.
  # ===========================================================================
  verify-tucaken-edge:
    name: Verify Tucaken Edge
    needs: [setup, deploy-tucaken-edge]
    if: |
      always()
      && needs.deploy-tucaken-edge.result == 'success'
      && needs.setup.outputs.tucaken-enabled == 'true'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nelson-lamounier/cdk-monitoring/ci:latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify Tucaken edge resources
        env:
          CDK_ENV: ${{ inputs.cdk-environment }}
          AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
          TUCAKEN_IO_DOMAIN: ${{ vars.TUCAKEN_IO_DOMAIN || 'tucaken.io' }}
          TUCAKEN_COM_DOMAIN: ${{ vars.TUCAKEN_COM_DOMAIN || 'tucaken.com' }}
        shell: bash
        run: |
          set -euo pipefail

          IO_DIST_PARAM="/tucaken/${CDK_ENV}/edge/io-distribution-id"
          COM_DIST_PARAM="/tucaken/${CDK_ENV}/edge/com-distribution-id"
          IO_CERT_PARAM="/tucaken/${CDK_ENV}/edge/io-certificate-arn"
          COM_CERT_PARAM="/tucaken/${CDK_ENV}/edge/com-certificate-arn"

          echo "── SSM outputs ─────────────────────────────────────"
          for p in "$IO_DIST_PARAM" "$COM_DIST_PARAM" "$IO_CERT_PARAM" "$COM_CERT_PARAM"; do
            VAL=$(aws ssm get-parameter --name "$p" --region "$AWS_REGION" \
              --query 'Parameter.Value' --output text)
            echo "  $p = $VAL"
            [[ -n "$VAL" ]] || { echo "ERROR: missing $p"; exit 1; }
          done

          IO_DIST_ID=$(aws ssm get-parameter --name "$IO_DIST_PARAM" \
            --region "$AWS_REGION" --query 'Parameter.Value' --output text)
          COM_DIST_ID=$(aws ssm get-parameter --name "$COM_DIST_PARAM" \
            --region "$AWS_REGION" --query 'Parameter.Value' --output text)

          echo "── CloudFront distribution states ──────────────────"
          for ID in "$IO_DIST_ID" "$COM_DIST_ID"; do
            STATUS=$(aws cloudfront get-distribution --id "$ID" \
              --query 'Distribution.Status' --output text)
            ENABLED=$(aws cloudfront get-distribution --id "$ID" \
              --query 'Distribution.DistributionConfig.Enabled' --output text)
            echo "  $ID  status=$STATUS  enabled=$ENABLED"
            [[ "$ENABLED" == "True" ]] || { echo "ERROR: distribution $ID disabled"; exit 1; }
          done

          echo "── ACM certificate states (us-east-1) ──────────────"
          IO_CERT_ARN=$(aws ssm get-parameter --name "$IO_CERT_PARAM" \
            --region "$AWS_REGION" --query 'Parameter.Value' --output text)
          COM_CERT_ARN=$(aws ssm get-parameter --name "$COM_CERT_PARAM" \
            --region "$AWS_REGION" --query 'Parameter.Value' --output text)
          for ARN in "$IO_CERT_ARN" "$COM_CERT_ARN"; do
            STATUS=$(aws acm describe-certificate --certificate-arn "$ARN" \
              --region us-east-1 --query 'Certificate.Status' --output text)
            echo "  $ARN  status=$STATUS"
            [[ "$STATUS" == "ISSUED" ]] || { echo "ERROR: cert $ARN not ISSUED"; exit 1; }
          done

          echo "[OK] Tucaken edge resources healthy"

  # ===========================================================================
  # Deploy Observability Stack (2f) — CloudWatch Pre-Deployment Dashboard
  #
  # Runs in parallel with Golden AMI, SSM Automation, and API stacks.
  # Depends only on Base stack (SSM parameters must exist).
  # Cost: $3.00/month — all metrics are standard AWS service metrics.
  # ===========================================================================
  deploy-observability:
    name: Deploy Observability
    needs: [setup, deploy-base, verify-base-stack]
    if: >-
      always()
      && needs.deploy-base.result == 'success'
      && needs.verify-base-stack.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.observability-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}

      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Deploy Platform RDS Stack (2e) — PostgreSQL 18 + Secrets Manager + SSM
  #
  # Runs in parallel with ControlPlane, Observability, and API stacks.
  # Depends only on Base stack (SharedVpc subnets must exist).
  # RDS first-creation can take up to 15 minutes — timeout set to 25 min.
  # K8s pods connect via PgBouncer (ArgoCD wave 2/3 in kubernetes-platform).
  # ===========================================================================
  deploy-platform-rds:
    name: Deploy Platform RDS
    needs: [setup, deploy-base, verify-base-stack]
    if: >-
      always()
      && needs.deploy-base.result == 'success'
      && needs.verify-base-stack.result == 'success'
    uses: ./.github/workflows/_deploy-stack.yml
    with:
      stack-name: ${{ needs.setup.outputs.platform-rds-stack }}
      project: kubernetes
      environment: ${{ inputs.environment }}
      aws-region: ${{ vars.AWS_REGION || 'eu-west-1' }}
      require-approval: ${{ inputs.require-approval }}
      outputs-directory: "deployment-outputs"
    secrets: inherit

  # ===========================================================================
  # Deployment Failure Alert (staging/production only)
  #
  # For K8s/GitOps environments, automated CFn rollbacks on stateful infra
  # (EC2 nodes running ArgoCD) risk split-brain state. Instead we:
  #   1. Halt the pipeline
  #   2. Preserve artifacts and diagnostics
  #   3. Alert engineers with actionable context
  #   4. Let them "roll forward" with a targeted fix
  # ===========================================================================
  deployment-failure-alert:
    name: "Deployment Failed — Alert"
    needs:
      [
        setup,
        verify-bff-ecr,
        deploy-controlplane,
        smoke-test-ami-refresh,
        deploy-general-pool,
        deploy-monitoring-pool,
        deploy-api,
        deploy-edge,
        verify-edge-stack,
        seed-tucaken-secret,
        deploy-tucaken-edge,
        verify-tucaken-edge,
        deploy-observability,
        deploy-platform-rds,
        verify-launch-template-ami,
      ]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}
    if: |
      always()
      && (
        needs.verify-bff-ecr.result == 'failure'
        || needs.deploy-controlplane.result == 'failure'
        || needs.smoke-test-ami-refresh.result == 'failure'
        || needs.deploy-general-pool.result == 'failure'
        || needs.deploy-monitoring-pool.result == 'failure'
        || needs.deploy-api.result == 'failure'
        || needs.deploy-edge.result == 'failure'
        || needs.verify-edge-stack.result == 'failure'
        || needs.seed-tucaken-secret.result == 'failure'
        || needs.deploy-tucaken-edge.result == 'failure'
        || needs.verify-tucaken-edge.result == 'failure'
        || needs.deploy-observability.result == 'failure'
        || needs.deploy-platform-rds.result == 'failure'
        || needs.verify-launch-template-ami.result == 'failure'
      )
      && inputs.cdk-environment != 'development'

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Environment and Dependencies
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        run: yarn install --immutable
        working-directory: ./

      - name: Configure AWS Credentials
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: "Generate Deployment Failure Report"
        continue-on-error: true
        env:
          DEPLOY_ENVIRONMENT: ${{ inputs.environment }}
          COMMIT_SHA: ${{ needs.setup.outputs.commit-sha }}
          CDK_ENV: ${{ inputs.cdk-environment }}
          JOB_RESULTS: >-
            {
              "control-plane": "${{ needs.deploy-controlplane.result }}",
              "general-pool": "${{ needs.deploy-general-pool.result }}",
              "monitoring-pool": "${{ needs.deploy-monitoring-pool.result }}",
              "edge": "${{ needs.deploy-edge.result || 'skipped' }}"
            }
          RUN_URL: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          COMMIT_URL: "${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}"
        run: |
          just ci-failure-report \
            --stacks "${{ needs.setup.outputs.data-stack }},${{ needs.setup.outputs.base-stack }},${{ needs.setup.outputs.controlplane-stack }},${{ needs.setup.outputs.general-pool-stack }},${{ needs.setup.outputs.monitoring-pool-stack }},${{ needs.setup.outputs.appiam-stack }},${{ needs.setup.outputs.api-stack }},${{ needs.setup.outputs.edge-stack }}" \
            --region ${{ env.AWS_REGION }} \
            --ssm-prefix "/k8s/${{ inputs.cdk-environment }}"

  # ===========================================================================
  # Deployment Summary (TypeScript)
  # ===========================================================================
  summary:
    name: Deployment Summary
    needs:
      - setup
      - verify-bff-ecr
      - deploy-data
      - deploy-controlplane
      - smoke-test-ami-refresh
      - deploy-general-pool
      - deploy-monitoring-pool
      - deploy-api
      - deploy-edge
      - verify-edge-stack
      - seed-tucaken-secret
      - deploy-tucaken-edge
      - verify-tucaken-edge
      - deploy-observability
      - deploy-platform-rds
      - verify-launch-template-ami
      - deployment-failure-alert
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always()
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write # OIDC for DescribeStacks verification
      contents: read

    steps:
      # Guard: if setup was cancelled/failed, write minimal fallback
      - name: Check Prerequisites
        id: prereqs
        run: |
          if [ "${{ needs.setup.result }}" != "success" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## Pipeline Aborted

          **Reason**: Setup job did not complete successfully (`${{ needs.setup.result }}`)
          **Commit**: `${{ github.sha }}`
          **Environment**: ${{ inputs.environment }}

          No stacks were deployed. Check the setup job logs for details.
          EOF
            echo "::warning::Setup did not succeed (${{ needs.setup.result }}), skipping full summary"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout
        if: steps.prereqs.outputs.skip != 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Environment and Dependencies
        if: steps.prereqs.outputs.skip != 'true'
        uses: ./.github/actions/setup-node-yarn
        with:
          node-version: ${{ needs.setup.outputs.node-version }}

      - name: Install Dependencies
        if: steps.prereqs.outputs.skip != 'true'
        run: yarn install --immutable
        working-directory: infra

      - name: Configure AWS Credentials
        if: steps.prereqs.outputs.skip != 'true'
        uses: ./.github/actions/configure-aws
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Audit CloudWatch Log Groups
        if: steps.prereqs.outputs.skip != 'true'
        continue-on-error: true
        run: just ci-log-audit --region ${{ env.AWS_REGION }}

      - name: Generate Summary
        if: steps.prereqs.outputs.skip != 'true'
        env:
          DEPLOY_DATA_RESULT: ${{ needs.deploy-data.result }}
          DEPLOY_CONTROLPLANE_RESULT: ${{ needs.deploy-controlplane.result }}
          DEPLOY_GENERAL_POOL_RESULT: ${{ needs.deploy-general-pool.result || 'skipped' }}
          DEPLOY_MON_POOL_RESULT: ${{ needs.deploy-monitoring-pool.result || 'skipped' }}
          DEPLOY_API_RESULT: ${{ needs.deploy-api.result || 'skipped' }}
          DEPLOY_EDGE_RESULT: ${{ needs.deploy-edge.result || 'skipped' }}
          VERIFY_EDGE_RESULT: ${{ needs.verify-edge-stack.result || 'skipped' }}
          SEED_TUCAKEN_SECRET_RESULT: ${{ needs.seed-tucaken-secret.result || 'skipped' }}
          DEPLOY_TUCAKEN_EDGE_RESULT: ${{ needs.deploy-tucaken-edge.result || 'skipped' }}
          VERIFY_TUCAKEN_EDGE_RESULT: ${{ needs.verify-tucaken-edge.result || 'skipped' }}
          DEPLOY_OBSERVABILITY_RESULT: ${{ needs.deploy-observability.result || 'skipped' }}
          DEPLOY_PLATFORM_RDS_RESULT: ${{ needs.deploy-platform-rds.result || 'skipped' }}
          VERIFY_RESULT: "moved-to-ssm-pipeline"
          SMOKE_TESTS_RESULT: ${{ needs.smoke-test-ami-refresh.result || 'skipped' }}
          ALERT_RESULT: ${{ needs.deployment-failure-alert.result || 'skipped' }}
          COMMIT_SHORT_SHA: ${{ needs.setup.outputs.commit-short-sha }}
        run: just ci-summary kubernetes ${{ inputs.cdk-environment }}
