# @format
# ArgoCD Application: Monitoring Stack
#
# Points ArgoCD at the monitoring Helm chart directory in Git.
# ArgoCD recognizes this as a Helm-type source and renders the chart
# with values from values-development.yaml.
#
# Secrets (Grafana password, GitHub token) are pre-seeded by the bootstrap orchestrator
# on first boot via deploy.py. ArgoCD manages all declarative Helm-rendered
# manifests from Day-2 onwards.
#
# Credential: SSH Deploy Key (read-only) — stored in K8s secret
# via bootstrap-argocd.sh from SSM parameter.

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  annotations:
    kubernetes.io/description: "ArgoCD application for the monitoring stack (Prometheus, Grafana, Loki, Tempo)"
    argocd.argoproj.io/sync-wave: "3"  # Wave 3: Platform Observability — must be healthy before Business Apps (wave 5)
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  # Preserve bootstrap-injected secrets that deploy.py overwrites from SSM.
  # selfHeal: true would otherwise revert these back to the Helm placeholder
  # on every ArgoCD sync cycle, locking users out of Prometheus.
  ignoreDifferences:
    - kind: Secret
      name: prometheus-basic-auth-secret
      namespace: monitoring
      jsonPointers:
        - /data

  source:
    repoURL: git@github.com:Nelson-Lamounier/cdk-monitoring.git
    # PRODUCTION: Change to 'main' when deploying to production.
    targetRevision: develop
    path: kubernetes-app/platform/charts/monitoring/chart
    helm:
      valueFiles:
        - values-development.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
